data_path: "${path_walt}/cam${cam_walt}/week${week_walt}"
bank_path: "${dataset.data_path}/bank"
image_path: "${dataset.bank_path}/images"
label_path: "${dataset.bank_path}/labels_${teacher}"
extension: "jpg"
basename: "WALT"
train_path: "train"
name: "${dataset.basename}-cam${cam_walt}-week${week_walt}"
