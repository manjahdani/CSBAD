data_path: "${data_root_folder}/cam${cam}/week${week}"
bank_path: "${dataset.data_path}/bank"
image_path: "${dataset.bank_path}/images"
label_path: "${dataset.bank_path}/labels_${teacher}"
basename: "WALT"
train_path: "train"
name: "${dataset.basename}-cam${cam}-week${week}"
