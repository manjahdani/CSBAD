weights: "${project_dir}/yolov8/yolov8n.pt"
name: "${dataset.name}_yolov8n_${strategy.name}_${teacher}"
entity: "trail22kd"
project: "multistream"
epochs: 100
batch: 16
teacher: ${teacher}
data: "${project_dir}/experiments/model/data.yaml"
