weights: "${project_dir}/yolov8/yolov8n.pt"
name: "${dataset.name}_yolov8n_${strategy.name}"
entity: "YourEntity"
project: "${dataset.basename}"
epochs: 100
batch: 16
data: "${project_dir}/experiments/model/data.yaml"