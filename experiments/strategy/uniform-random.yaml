_target_: subsampling.strategies.uniform_stream_based

image_folder_path: "${dataset.image_path}"
image_labels_path: "${dataset.bank_path}/labels_yolov8n_w_conf"
n: ${n_samples}
sampling_rate: 0.10
seed: ${seed}
name: "uniform-random-${seed}_stream-based_${n_samples}"