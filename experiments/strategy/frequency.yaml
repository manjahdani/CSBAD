_target_: subsampling.strategies.strategy_frequency

image_folder_path: "${dataset.image_path}"
image_labels_path: "${dataset.bank_path}/labels_${student}_w_conf"
bank_folder_path: "${dataset.bank_path}"
imgExtension: null # To be filled
n_groups: 10
n: ${n_samples}

name: "frequency_pool-based_${n_samples}"
