_target_: strategy.subsampling.uniform_stream_based

image_labels_path: "${dataset.bank_path}/labels_yolov8n_w_conf"
n: ${n_samples}

sampling_rate: 0.10
seed: 0
name: "uniform-streambased_${n_samples}"