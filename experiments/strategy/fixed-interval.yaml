_target_: subsampling.strategies.strategy_fixed_interval

image_folder_path: "${dataset.image_path}"
imgExtension: null #To be filled
val_size: ${val.val_set_size}
n: ${n_samples}

name: "fixed-interval_pool-based_${n_samples}"
