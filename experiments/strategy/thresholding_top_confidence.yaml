_target_: subsampling.strategies.thresholding_top_confidence

image_labels_path: "${dataset.bank_path}/labels_yolov8n_w_conf"
n: ${n_samples}
aggregation_function: "sum"
sampling_rate: 0.10
warmup_length: 720
name: "thresholhed-top-confidence-${strategy.aggregation_function}_stream-based_${n_samples}"
